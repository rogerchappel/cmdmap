# cmdmap docs

- `PRD.md` describes the product target.
- `TASKS.md` records the build contract.
- `ORCHESTRATION.md` records OSS factory boundaries.
- Generated command maps can be written here with `cmdmap scan . --out docs/COMMANDS.md`.
