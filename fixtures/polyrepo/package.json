{
  "name": "polyrepo-fixture",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "dev": "vite --host 127.0.0.1",
    "release": "npm publish"
  }
}
