[project]
name = "polyrepo-fixture"

[project.scripts]
fixture-cli = "polyrepo.cli:main"

[tool.poetry.scripts]
fixture-admin = "polyrepo.admin:main"

[tool.poe.tasks]
fixture-test = "pytest"
fixture-secret = "python scripts/read_secret.py"
